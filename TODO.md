# TODO

- [ ] Fix `lib/view/home/home_view.dart` compile errors (duplicate `initState`, misplaced overrides/build, undefined list types and widget rendering issues).
- [ ] Ensure `recommendedItems`/`mostOrderedItems` are defined with correct types and used consistently.
- [ ] Ensure `userLocation` is defined in the state and referenced correctly.
- [ ] Run `flutter analyze` / `flutter test` / `flutter build` to confirm build success.

